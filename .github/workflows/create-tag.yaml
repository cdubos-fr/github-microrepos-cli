name: create-release
on:
  push:
    branches:
    - release

jobs:
  create-tags:
    uses: cdubos-fr/reusable-workflows/.github/workflows/python-tag.yml@v0
    with:
      python-version: 3.10
      module-name: "galaxy_census_backend"
    secrets:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
